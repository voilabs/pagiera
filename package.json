{
  "name": "pagiera-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Pagiera visual website builder monorepo",
  "homepage": "https://pagiera.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/voilabs/pagiera.git"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run package:build && bun run --cwd apps/example dev",
    "build": "bun run package:build && bun run --cwd apps/example build",
    "check": "bun run package:check && bun run package:build && bun run --cwd apps/example typecheck",
    "lint": "bun run --cwd apps/example lint",
    "format": "bun run --cwd apps/example format",
    "package:build": "bun run --cwd packages/pagiera build",
    "package:check": "bun run --cwd packages/pagiera check",
    "package:pack": "bun run package:build && npm pack --workspace pagiera --ignore-scripts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.9",
    "typescript": "^7"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=20"
  },
  "trustedDependencies": [
    "sharp",
    "unrs-resolver"
  ]
}
