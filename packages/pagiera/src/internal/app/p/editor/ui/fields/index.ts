/**
 * The inspector's controls.
 *
 * - `style.ts`   — the four class strings every field is built from.
 * - `group.tsx`  — sections, rows, and the override marks.
 * - `choice.tsx` — dropdowns and segmented bars.
 * - `size.tsx`   — width and height.
 * - `number.tsx` — typed, dragged and slid numbers.
 * - `text.tsx`   — single lines, paragraphs and code.
 * - `color.tsx`  — swatches.
 */

export * from "./choice";
export * from "./color";
export * from "./group";
export * from "./number";
export * from "./size";
export * from "./style";
export * from "./text";
